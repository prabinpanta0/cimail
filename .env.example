# Supabase (Project Settings → API)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_DEFAULT_KEY=


# Gmail API OAuth (Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional: if not set, the app will use the refresh token stored in Supabase after first owner login.
GOOGLE_REFRESH_TOKEN=

# Session cookie signing secret (random long string)
SESSION_SECRET=

# Your addresses
MAIL_ME_ADDRESS=me@domain.com
MAIL_NOREPLY_ADDRESS=noreply@domain.com

# Simple shared secrets for protected endpoints
# /api/send requires x-api-key
API_KEY=change-me
# /api/sync requires x-sync-secret
SYNC_SECRET=change-me

# Cloudflare Email Worker -> app ingestion
EMAIL_ROUTING_SECRET=change-me
